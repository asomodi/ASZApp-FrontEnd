import { AlbumSearchPipe } from './album-search.pipe';

describe('AlbumSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
